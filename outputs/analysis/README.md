# Analysis Outputs

This folder is intentionally separate from `outputs/final_plots/`.

It contains the documented post-freeze analysis artifacts:

- `fig6_multiseed_all600_seeds42_46/`
- `fig3_kl_bshs_seedmean_scatter_20seeds_all600/`
- `fig3_kl_bshs_seedmean_scatter_10seeds_all600/`
- `fig3_kl_bshs_seedmean_scatter/`

Selected reference artifacts are:

- the Fig6 multiseed recovery rerun
- the Fig3 KL-BSHS dual-axis boxplot with 20 seeds and all models trained for 600 steps/epochs

Each selected output directory contains its own `README.md`, and the selected Fig6 and Fig3 runs both include `RUN_CONFIG.json`.

The kept analysis reruns are generated by self-contained scripts and do not depend on runtime git-history loading.

Integrity metadata for the kept analysis files is stored in:

- `ARTIFACT_MANIFEST.csv`
